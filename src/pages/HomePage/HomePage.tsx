import { GameMap, TargetsList } from '../../components';

const HomePage = () => {
    return (
        <div className='flex'>
            <GameMap />
        </div>
    );
};

export default HomePage;
